
  usage: basic [options] [files]

    Quick overview of what we do here.

  options:

    -a, --ambiguous
      possibly ambiguous argument.

    -A, --arbitrary
      an arbitrary word, also begninning with `a`.

    -N, --name <name>
      the name of a thing.

    -p, --processes <count>
      number of processes to run in parallel.

    -c, --config <key=value> @
      set configuration varaibles.

    -h, --help
      display this help message.

  description:

  Here's a description.

  :usage
